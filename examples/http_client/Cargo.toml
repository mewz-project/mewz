[package]
name = "http_client"
version = "0.1.0"
edition = "2021"

[dependencies]
hyper_wasi = { version = "0.15.2", features = ["client", "http1", "tcp"] }
tokio_wasi = { version = "1", features = ["rt", "macros", "net", "io-util"] }
